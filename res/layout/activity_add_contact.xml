<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AddContact" >

    <Button
        android:id="@+id/contactAddButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="contactAdd"
        android:text="@string/kontaktauslistbutton" />

    <Button
        android:id="@+id/kontaktmanuell"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/contactAddButton"
        android:layout_alignLeft="@+id/contactAddButton"
        android:layout_alignRight="@+id/contactAddButton"
        android:text="@string/kontaktmanuellbutton" 
        android:onClick="contactmanualAdd"
        />

</RelativeLayout>
